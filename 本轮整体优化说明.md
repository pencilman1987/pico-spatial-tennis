# PICO 空间网球整体优化说明

## 本轮目标

- 保持 PICO 手柄为主要操作方式，增加右手柄物理挥拍识别，并保留按键回退。
- 保持完全单机，不加入联网对战或联机依赖。
- 保持程序化 3D 角色和游戏元素美术，不使用四个角色 GLB。
- 提升击球手感、对局可读性、结算激励和空间画面完成度。

## 已完成优化

### 玩法与手感

- 新增右手柄位移阈值挥拍检测，右扳机、A、B 继续作为可预测的按键回退。
- 新增 `GOOD / PERFECT` 击球质量：球拍与球更居中时判定完美击球，奖励额外能量和更有攻击性的球速。
- AI 失误不再直接送分，而是返回更慢、更居中的“机会球”，让得分由玩家完成。
- 新增最佳回合记录，换发球时不会丢失。
- 结算新增三星评价，综合胜负、分差和最佳回合。

### 3D 场景与虚拟形象

- 球员新增待机律动、横向移动倾斜和正弦缓动挥拍，降低“静态摆件”感。
- 新增球路拖尾、击球冲击环、落点环和必杀蓄满脚下光环。
- 保留三种 AI 轮廓和配色，但运行时只创建当前对手，不再同时常驻三套形象。
- 观众与长椅装饰减半，保留杯赛氛围同时降低实体和渲染开销。

### UI 与信息层级

- 菜单升级为 `NEON CUP · SOLO ARENA` 杯赛视觉，明确“挥拍/按键击球、单机、无需联网”。
- HUD 新增回合数、击球质量、必杀状态和最佳回合，并放大空间显示尺寸。
- 新增得分/失分过场卡，结算卡增加星级和对局数据。
- 交互继续使用 PICO SpatialUI 官方 `Button` 与悬停效果，所有颜色路由 `PicoTheme` 主题角色。

## 验收结果

- `testDebugUnitTest` + `assembleDebug`：通过。
- 单元测试：14 项通过，0 失败，0 错误。
- SpatialUI 设计规范校验：PASS，0 错误，0 警告。
- PICO 模拟器：APK 安装成功，Stage 可见且聚焦，首帧提交成功，崩溃缓冲区为空。
- 热启动 12 秒采样：应用平均 58.08 FPS，应用 CPU 平均 7.84%，无 jank window。

## 仍需真机验证

- 当前 PICO 模拟器不提供右手柄追踪，物理挥拍阈值、球拍姿态偏移和实际手感必须在 PICO 真机上复测。
- 模拟器公共空间渲染池会报 91% 容量警告，该警告在系统启动器中也会出现；应以真机 GPU/帧时间采样决定是否继续减面或减少特效。
- 下一个高价值品质项是击球震动和空间音效，需基于真机事件时序调校。

## 交付物

- Debug APK：`app/build/outputs/apk/debug/app-debug.apk`
- 最终模拟器截图：`.scratch/quality-pass/final-optimized.png`
- 热启动性能报告：`.scratch/quality-pass/perf-report-warm.json`

原始浏览器项目目录 `../网球游戏` 未被修改。
